# Input Format

- the core script expects a Chrome bookmarks JSON source by default
- the selected bookmark folder can be passed by short folder name or full bookmark path
- the output is a Markdown note written to a caller-provided output directory
- public-facing docs should focus on the core script, not on private convenience wrappers
