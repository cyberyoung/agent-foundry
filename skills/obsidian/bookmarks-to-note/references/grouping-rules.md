# Grouping Rules

- `rules` mode uses configured topic rules only
- `llm` mode relies on model-generated grouping
- `hybrid` mode combines deterministic rules with model assistance and optional rule learning
- flat bookmark folders benefit most from grouping; nested folders may already have enough structure
- public docs should make the default grouping behavior explicit and explain how to disable it
