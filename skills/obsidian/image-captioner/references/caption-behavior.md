# Caption Behavior

- the skill works on one note at a time
- only missing captions are targeted by default
- existing captions are skipped unless `--force` is used
- inserted captions should preserve local indentation and list structure
