# Dependency Notes

- required: `python3`, `python-docx`
- optional cleanup may still be needed for layout-heavy documents
- public-facing docs should distinguish structural fidelity from exact visual fidelity
