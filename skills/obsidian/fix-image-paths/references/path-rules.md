# Path Rules

- images referenced by a note should end up under that note's local `assets/` directory
- images already under the expected assets area are left untouched
- moved files should keep predictable names and updated embeds should point to the new location
- unresolved files and edge-case embed patterns should be reviewed manually
