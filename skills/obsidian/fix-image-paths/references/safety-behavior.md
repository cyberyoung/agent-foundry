# Safety Behavior

- the skill edits the selected note in place
- the skill may move image files into a note-local `assets/` layout
- use `--dry-run` first when testing a new note or folder pattern
- review the resulting note diff and asset tree after execution
